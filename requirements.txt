# SCS-C03 study-course scripts — pinned dependencies.
# Exact pins (supply-chain hardening): a yanked/republished release can't silently
# resolve to a different version. Bump deliberately, then re-run the scripts.
# Install:  python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
boto3==1.42.48
botocore==1.42.48
